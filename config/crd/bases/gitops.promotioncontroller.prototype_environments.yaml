---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.1
  creationTimestamp: null
  name: environments.gitops.promotioncontroller.prototype
spec:
  group: gitops.promotioncontroller.prototype
  names:
    kind: Environment
    listKind: EnvironmentList
    plural: environments
    singular: environment
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Environment is the Schema for the environments API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: EnvironmentSpec defines the desired state of Environment
            properties:
              source:
                description: Source defines the source of the environment.
                properties:
                  path:
                    description: The path to the directory which represents the environment.
                    type: string
                  provider:
                    description: The Git provider. Required if you want to do pull
                      requests.
                    type: string
                  secretRef:
                    description: SecrefRef reference the secret that contains the
                      API token for the Git provider. Secret is of type generic and
                      has the API token string stored in the "token" key. Required
                      if the repository is private. Required if you want to raise
                      Pull Requests against it.
                    properties:
                      name:
                        description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          TODO: Add other useful fields. apiVersion, kind, uid?'
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  url:
                    description: The clone URL of the Git repository (HTTPS).
                    type: string
                required:
                - path
                - url
                type: object
            required:
            - source
            type: object
          status:
            description: EnvironmentStatus defines the observed state of Environment
            properties:
              localClonePath:
                description: The path to the local clone of the Environment.
                type: string
              ready:
                description: Indicates whether the Environment is ready for promotion.
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
