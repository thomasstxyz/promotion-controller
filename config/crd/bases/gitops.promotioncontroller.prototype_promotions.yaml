---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.1
  creationTimestamp: null
  name: promotions.gitops.promotioncontroller.prototype
spec:
  group: gitops.promotioncontroller.prototype
  names:
    kind: Promotion
    listKind: PromotionList
    plural: promotions
    singular: promotion
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Promotion is the Schema for the promotions API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: PromotionSpec defines the desired state of Promotion
            properties:
              copy:
                description: Copy defines a list of copy operations to perform.
                items:
                  description: CopyOperation defines a file/directory copy operation.
                  properties:
                    source:
                      description: The source path to copy from.
                      type: string
                    target:
                      description: The target path to copy to.
                      type: string
                  required:
                  - source
                  - target
                  type: object
                type: array
              sourceEnvironment:
                description: The source environment to promote from.
                type: string
              strategy:
                description: Strategy defines the strategy to use when promoting.
                properties:
                  pullRequest:
                    properties:
                      body:
                        description: The body of the pull request.
                        type: string
                      provider:
                        description: The Git provider for the pull request.
                        type: string
                      secretRef:
                        description: SecrefRef reference the secret that contains
                          the API token for the Git provider.
                        properties:
                          name:
                            description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              TODO: Add other useful fields. apiVersion, kind, uid?'
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                      title:
                        description: The title of the pull request.
                        type: string
                    required:
                    - body
                    - provider
                    - secretRef
                    - title
                    type: object
                required:
                - pullRequest
                type: object
              targetEnvironment:
                description: The target environment to promote to.
                type: string
            required:
            - copy
            - sourceEnvironment
            - strategy
            - targetEnvironment
            type: object
          status:
            description: PromotionStatus defines the observed state of Promotion
            properties:
              pullRequestBranch:
                description: Pull request branch name.
                type: string
              pullRequestNumber:
                description: Pull request number.
                type: integer
              pullRequestStatus:
                description: Pull request status.
                type: string
              pullRequestTitle:
                description: Pull request title.
                type: string
              pullRequestURL:
                description: Pull request URL.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
